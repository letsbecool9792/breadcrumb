<?xml version="1.0" encoding="utf-8"?>
<!-- The launcher shortcut's icon: an adaptive icon, so the launcher shapes it
     like the app's own. Hand-built, like the tile icon, not generated. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/brand_ink" />
    <foreground android:drawable="@drawable/ic_shortcut_keep_foreground" />
</adaptive-icon>
